WITH Ada.Integer_Text_IO, Ada.Text_IO;
USE Ada.Integer_Text_IO, Ada.Text_IO;

PACKAGE BODY Personas IS

   PROCEDURE Escribir_Personas (
         V : T_Vector_Personas) IS
   BEGIN
      FOR I IN V'RANGE LOOP
         Put(I, 3);
         Put(": ");
         Put(V(I).NombreApellidos);
         Put(" - ");
         Put(V(I).Ciudad);
         Put("- ");
         Put(Boolean'Image(V(I).Trabajando));
         Put(V(I).Edad, 3);
         New_Line;
      END LOOP;

   END Escribir_Personas;


   PROCEDURE Actualizar_Ciudad (
         V  : IN OUT T_Vector_Personas;
         C1,
         C2 :        String) IS

   BEGIN
      FOR I IN V'RANGE LOOP
         IF V(I).Ciudad = C1 THEN
            V(I).Ciudad := C2;
         END IF;
      END LOOP;

   END Actualizar_Ciudad;

   FUNCTION Esta_Persona (
         V : IN     T_Vector_Personas;
         P :        T_Persona)
      RETURN Boolean;

   BEGIN
      WHILE i<=v'last and then V(I).NombreApellidos /= P(I) THEN
      end loop;
   END esta_persona;

END Personas;