WITH Ada.Text_IO;
USE Ada.Text_IO;

PACKAGE LAB6_Lista_Personas IS

   TYPE T_Persona IS
      RECORD
         NombreApellidos : String (1 .. 25);
         Ciudad          : String (1 .. 12);
         Trabajando      : Boolean;
         Edad            : Integer;
         Sueldo          : Float;
      END RECORD;

   TYPE T_Vector_Personas IS ARRAY (Integer RANGE <>) OF T_Persona;

   TYPE T_Lista_Personas IS
      RECORD
         Lista : T_Vector_Personas (1 .. 10);
         Cont  : Integer RANGE 0 .. 10;
      END RECORD;

   ---- PERSONA -----
   FUNCTION Cargar_Persona (
         F : File_Type)
     RETURN T_Persona;
   -- entrada: Tres String Nom, Ci, Tra, un entero Edad y un float Sueldo (F)
   -- pre: Los valores corresponden a la informacion de una persona. F fichero abierto
   -- pos: Devuelve los datos Nom, Ci, Tra, Edad y Sueldo en una tipo de
   --      dato persona.

   PROCEDURE Escribir_Persona (
         P : T_Persona);
   -- salida: una Persona (SE)
   -- pos: Escribe la información de una persona en una línea.


   ---- VECTOR_PERSONA -----

   PROCEDURE Escribir_Personas (
         V : T_Vector_Personas);
   -- salida Secuencia de Personas S (SE)
   -- pos: S está formada por las personas de V. Toda la información de la persona en una línea.

   ---- LISTA_PERSONA -----
   PROCEDURE Cargar_Personas (
         S :        String;
         L :    OUT T_Lista_Personas);
   -- entrada: Secuencia de Personas (Fichero S)
   -- pre: Cada valor correspondiente a cada persona está en una línea. S es nombre de fichero que
   --   como mucho hay tantas personas como la capacidad de de L.
   -- post: devuelve una lista con las personas de S.


   PROCEDURE Escribir_Personas (
         L : T_Lista_Personas);
   -- salida Secuencia de Personas S (SE)
   -- pos: S está formada por las personas de V. Toda la información de la persona en una línea.

   --CUANTOS_CIUDAD--

   FUNCTION Cuantos_Ciudad (L: T_Lista_Personas; S:string) RETURN Integer;
   --pos: todas las posiciones del string ciudaque no es el nombre de la ciudad estan rellenadas por ``

END LAB6_Lista_Personas;
